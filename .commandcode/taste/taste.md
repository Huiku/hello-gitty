# User Taste — hello-gitty

- Values UI contrast/legibility strongly; will push back when light-theme colors are too low-contrast (e.g. calls out a near-white text on light button, then a bright green on light background as unreadable). When fixing, prefers a dedicated semantic variable (e.g. `--on-accent` for button-on-brand text) over repurposing an existing global one, to avoid breaking other usages. Confidence: 0.85
